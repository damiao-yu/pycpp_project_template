cmake_minimum_required(VERSION 3.16)

# Add C++ example executables here if needed, for example:
# add_executable(example_cpp example.cpp)
# target_link_libraries(example_cpp PRIVATE ${PROJECT_NAME}_core)
